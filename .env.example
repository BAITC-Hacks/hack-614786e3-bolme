# Copy to .env.local. Without a key the app works and shows the deterministic engine report.
OPENAI_API_KEY=
# Model for the grounded AI report (Responses API + Structured Outputs).
OPENAI_MODEL=gpt-6-luna
